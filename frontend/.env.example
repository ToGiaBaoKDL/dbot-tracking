# Frontend .env — Next.js environment variables
# This file is separate from root .env because Next.js reads env from its own directory.
#
# (*) Required values:
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
